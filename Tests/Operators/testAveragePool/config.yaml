averagepool:
  # Input shape (batch, channels, height, width)
  input_shape: [1, 8, 16, 16]

  # Pooling parameters
  kernel_shape: [2, 2]
  strides: [2, 2]
  pads: [0, 0, 0, 0]  # [top, left, bottom, right]
  count_include_pad: 0
  ceil_mode: 0

  # ONNX opset version
  opset_version: 13
